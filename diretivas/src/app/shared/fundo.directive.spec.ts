import { FundoDirective } from './fundo.directive';

describe('FundoDirective', () => {
  it('should create an instance', () => {
    const directive = new FundoDirective();
    expect(directive).toBeTruthy();
  });
});
