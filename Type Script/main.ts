var palavra = 'minha variavel';

function soma(x,y){
  return x + y ;
}

let array = [1,3,5,7,9]; 
array.map(function (valor){ // Programacao Funcional em JS

  return valor * 2;

});

array.map(valor  => valor * 2); // Programacao Funcional em ES2015
                 