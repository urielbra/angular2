var palavra = 'minha variavel';
function soma(x, y) {
    return x + y;
}
var array = [1, 3, 5, 7, 9];
array.map(function (valor) {
    return valor * 2;
});
array.map(function (valor) { return valor * 2; }); // Programacao Funcional em ES2015
